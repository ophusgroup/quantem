name: Ruff

on:
  pull_request:
    branches:
      - main

jobs:
  uv-ruff:
    name: ruff
    runs-on: ubuntu-latest

    steps:
        - uses: astral-sh/ruff-action@v3
          with:
            src: ../
            args: check --select I --exclude __init__.py
        - run: ruff format
